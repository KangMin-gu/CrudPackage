<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="email">

    <!-- 발송코드변경 -->
     <update id="sendstate" parameterType="saas.crud.crm.ce.MailDto">
        UPDATE TB150020 SET RLTCODE = #{rltcode} WHERE EMAILLOGID = #{emaillogid};
     </update>

    <!--예약 / 다이렉트 전부가져오기 -->
    <select id="allTarget" parameterType="saas.crud.crm.ce.MailDto" resultType="hashMap">
      	SELECT TOP 100 * FROM t_mail WHERE RLTCODE = 0 AND '2018-12-12 16:15:12' <![CDATA[>=]]> RLTDATE;
    </select>

</mapper>